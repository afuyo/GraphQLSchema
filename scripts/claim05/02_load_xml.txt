call apoc.xml.import('file:///tmp/ACORD.xml');


match (scopeNode:Scope)
with collect(scopeNode.Name) as scopeNodeNameList
match(n) 
where (n.type = "uml:DataType" and n._name = "packagedElement" and n.name in scopeNodeNameList) 
or
 (n.type="uml:Class" and n._name = "packagedElement" and n.name in scopeNodeNameList) 

set n:AcordType return n;

